fn main() {
    println!("Hello, world!");
}

/*
 *Some rules must be explained befdore we go foward:
 *If a trait doesn't exists in the scope of the function,
 *it then must be imported.
 *
 * The implementation of the trait must be in the same 
 * crate as the trait or type.  
 */